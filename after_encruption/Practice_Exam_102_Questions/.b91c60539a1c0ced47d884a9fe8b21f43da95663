Which statement best describes the following, displayed using the ls - la command?lrwxrwxrwx. 1 root root 35 Jul 8 2014 .fetchmailrc- > .configs/fetchmail/.fetchmailrc
A)  It is a file called .fetchmailrc that is linked using a symbolic link.
B)  It is a file called .configs/fetchmail/.fetchmailrc that is owned by lrwxrwxrwx.
C)  It is a directory called .fetchmailrc that is owned by user Jul.
D)  It is a local directory called .configs/fetchmail/.fetchmailrc.
A
A. The listing shows a symbolic linked file located in the current directory, linked to.configs/fetchmail/.fetchmailrc. The file is owned by the root user and rootgroup and was created on July 8, 2014.7.
